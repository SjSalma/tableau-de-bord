import React from "react"
import ReactDOM from "react-dom/client"
import MentalHealthDashboard from "./App"

const root = ReactDOM.createRoot(document.getElementById("root"))
root.render(
  <React.StrictMode>
    <MentalHealthDashboard />
  </React.StrictMode>
)
